import React from 'react';

/**
 * Этот файл был создан для устранения потенциальной ошибки сборки.
 * В данный момент он не используется в приложении.
 */
const MainLayout: React.FC<{ children?: React.ReactNode }> = ({ children }) => {
  return <>{children}</>;
};

export default MainLayout;
